<% if @reserve.errors.any? || @reserve_item.errors.any? %>
alert("not valid.")
<% else %>
alert("<%= @reserve_item.id %>")
 $(".cart-text").html("<%= escape_javascript(render 'layouts/cart_text') %>")
<% end %>
