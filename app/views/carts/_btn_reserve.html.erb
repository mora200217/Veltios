<% if @reserve_items.size != 0%>
<div class = "reserve-btn-cart">
 <%= button_to("Send Reservations", {action: "send_mail", controller: "reserve_items"}, {class: "btn btn-success"}) %>
</div>
<% end%>
